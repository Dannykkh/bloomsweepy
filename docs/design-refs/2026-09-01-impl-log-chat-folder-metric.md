# Implementation Log: 대화 폴더 용량과 공급자 오류

## Source Mode

- delta: 기존 `대화` 단일 작업면과 디자인 토큰을 보존한 국소 개선.
- 사용자 과업: 선택한 폴더의 크기와 드라이브 점유 정도를 질문 전에 확인하고, 공급자 전환 실패 원인을 이해한다.

## Prompt Contract

- 기존 범위 선택 행 안에서 폴더 이름 옆에 완료된 논리 용량과 드라이브 총용량 대비 비율을 표시한다.
- 검사 중 값은 `확인 중`으로 표시하고 확정 비율처럼 보이지 않게 한다.
- 공급자별 실행 제한을 유지하되 로컬 Ollama에는 더 긴 제한과 즉시 취소를 제공한다.
- 새 카드, 장식 효과, 파일 내용·전체 경로 전송은 추가하지 않는다.

## Changes

- `AssistantView.tsx`: 폴더 용량·드라이브 비율, 중복 제출 동기 가드, Ollama 대기 문구.
- `App.tsx`: 이미 계산 중인 `VolumeInfo`를 대화 화면에 전달.
- `App.css`: 14px 숫자 메타데이터와 좁은 컨테이너 재배치.
- `assistant_provider.rs`: Claude Code MCP 빈 설정 호환 수정, Ollama 10분 제한, 응답·오류 파일의 분리된 상한과 오류 분류.

## Verification

- TypeScript 타입 검사 통과.
- Rust 공급자 테스트 7개 통과.
- Claude Code 2.1.226에서 앱과 동일한 비대화형 호출 및 실제 앱 응답 확인.
- Ollama `gemma4:12b-it-qat` 비대화형 응답 확인.
- 1295×849 실제 Tauri 렌더에서 폴더 이름·용량·드라이브 비율의 겹침 없음 확인.

## Deferred

- 대화 세션·검사 스냅샷·최근 검색 이력의 SQLite 저장은 별도 데이터 보존 계약으로 구현한다.
- JSONL/NDJSON 공급자 스트리밍은 공급자별 지원과 오류 이벤트 형식을 정규화한 뒤 도입한다.
